= Instruction =

1. install some ruby libraries
 - openssl
 - json

2. setup config.yml

3. run twiprowl


= config.yml =

LogDir: <Log Directory>
Debug: <Debugging ouput to log. true/false >

Prowl:
 APIKey: <the Prowl API Key>
 ProxyHost: <HTTP Proxy host address if it's available>
 ProxyPort: <HTTP Proxy port number>
 ProxyUser: <Authentication username for HTTP Proxy if it's required>
 ProxyPass: <Authentication password for HTTP Proxy if it's required>

Accounts:
 -
  Application: <application name (optional)> 
  User: <Twitter username>
  Pass: <Twitter Password>
  Mentions: <true|false> # Mentions
  MentionsCount: <limit to get new mentions (int)>
  MentionsInterval: <check interval (min)>
  MentionsPriority: <-2 to 2> # prowl priority, default 0
  Direct: <true|false> # Direct Message
  DirectCount: <limit to get new DirectMessage (int)>
  DirectInterval: <check interval (min)>
  DirectPriority: <-2 to 2> # prowl priority, default 0
  UseProxy: <true|false> # use HTTP Proxy?
 - 
  <Other account config here>

= Instruction =

Version : 0.9.0

1. install some ruby libraries

 for Ruby 1.8
  - openssl
  - json
  - hmac-ruby

   $ sudo apt-get install libopenssl-ruby1.8 libjson-ruby1.8 libhmac-ruby1.8

 for Ruby 1.9.1
  - openssl
  - ruby-hmac (from rubygems)

   $ sudo apt-get install rubygems1.9.1 libopenssl-ruby1.9.1
   $ sudo gem1.9.1 install ruby-hmac

2. copy config.yml to ~/.twiprowl.conf and edit it.

3. run twiprowl


= config.yml =

LogDir: <Log Directory>
Debug: <Debugging ouput to log. true/false >

Prowl:
 APIKey: <the Prowl API Key>
 ProxyURL: <HTTP Proxy like http://user:pass@host:port/ >

Accounts:
 -
  Application: <application name (optional)> 
  User: <Twitter username>
  Pass: <Twitter Password>
   # You should remove this after successed xAuth process. 
  MentionsInterval: <check interval (sec)>
   # checking mentions, 0 for disable it, default: 300
  MentionsCount: <limit to get new mentions (int)>
  MentionsPriority: <-2 to 2>
   # prowl priority, default 0
  DirectInterval: <check interval (sec)>
   # checking direct messages, 0 for disable it, default: 600
  DirectCount: <limit to get new DirectMessage (int)>
  DirectPriority: <-2 to 2>
   # prowl priority, default 0
  RetweetsInterval: <check interval (sec)>
   # checking ReTweet (only 1st time), 0 for disable it, default: 300
  RetweetsCount: <limit to get new retweets (int)>
  RetweetsPriority: <-2 to 2>
   # prowl priority, default 0
  UseProxy: <true|false>
   # use HTTP Proxy to access Twitter API
  RateLimitThreshold: <0 to 150>
   # stop to access TwitterAPI when RateLimitStatus is under the value, default: 20
  RemotterInterval: <check interval (sec) for unfollow>
   # checking unfollow actions, 0 for disable it. default: 0
  RemotterPriority: <-2 to 2>
   # prowl priority, default 0
  MembershipInterval: <check interval (sec) for list membership>
   # checking list membership changes, 0 for disable it. default: 0
  MembershipPriority: <-2 to 2>
   # prowl priority, default 0
 - 
  <Other account config here>
